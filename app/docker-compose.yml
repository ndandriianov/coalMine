services:
  backend:
    build:
      context: backend
    ports:
      - "9091:9091"
    restart: unless-stopped

  frontend:
    build:
      context: frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: unless-stopped

